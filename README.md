# Travel Planner (NVIDIA NeMo Agent Toolkit + NVIDIA API)

Generates a trip itinerary using:
- NVIDIA NeMo Agent Toolkit (NAT) workflow + registered tool
- NVIDIA hosted OpenAI-compatible Chat Completions API

## Setup
```bash
git clone <repo-url>
cd travel-planner-nat
chmod +x setup.sh
./setup.sh
